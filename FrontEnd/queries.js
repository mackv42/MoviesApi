function makeQuery( x ){
	$.ajax({
		type: 'POST',
		
	})
}

$("#enterQuery").on('click', function(){
	//alert("This works");
	var query = {};
	var selected  = $("#querySelect option:selected").val();
	alert(selected);
	//if(query.)
});